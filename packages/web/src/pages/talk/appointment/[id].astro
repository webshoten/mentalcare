---
import AppointmentLayout from "../../../layouts/AppointmentLayout.astro";
import { AppointmentView } from "@/components/talk/appointment";

const { id } = Astro.params;
---

<AppointmentLayout title="セッション | MentalCare">
  <AppointmentView appointmentId={id!} client:only="react" />
</AppointmentLayout>
